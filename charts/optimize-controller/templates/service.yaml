apiVersion: v1
kind: Service
metadata:
  name: {{ include "optimize-controller.fullname" . }}
  labels:
    {{- include "optimize-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "optimize-controller.selectorLabels" . | nindent 4 }}
