apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "optimize-controller.fullname" . }}-test-connection"
  labels:
    {{- include "optimize-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "optimize-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
